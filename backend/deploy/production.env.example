# 프로덕션 환경변수 설정 예시
# 실제 배포 시에는 이 파일을 production.env로 복사하고 실제 값으로 변경

# 애플리케이션 설정
APP_NAME=OntoTrade API
APP_VERSION=1.0.0
DEBUG=false
ENVIRONMENT=production

# 서버 설정
HOST=0.0.0.0
PORT=8000

# CORS 설정 (프로덕션 도메인)
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Supabase 설정 (프로덕션 인스턴스)
SUPABASE_URL=https://your-prod-project.supabase.co
SUPABASE_ANON_KEY=your_production_anon_key
SUPABASE_SERVICE_KEY=your_production_service_key
SUPABASE_JWT_SECRET=your_production_jwt_secret

# 외부 API 키 (프로덕션용)
ALPHA_VANTAGE_API_KEY=your_production_alpha_vantage_key
TWELVE_DATA_API_KEY=your_production_twelve_data_key

# 보안 키 (프로덕션용 - 강력한 키 사용)
SECRET_KEY=your-super-strong-production-secret-key-128-characters-long

# 이메일 설정 (프로덕션 SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=noreply@your-domain.com
SMTP_PASSWORD=your_production_email_password

# 로깅 설정
LOG_LEVEL=INFO
SENTRY_DSN=your_sentry_dsn_for_error_tracking